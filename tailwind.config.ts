import type { Config } from "tailwindcss";

export default <Partial<Config>>{
    theme: {
    extend: {
      colors: {
        canvas: "#F5F6F8",
        chipActive: "#4254FF",
        elevatedButton: "#300863"
      }
    }
  }
}